#include<stdio.h>
void main()
{
    int x;
    printf("the is even and odd");
    scanf("%d",&x);
    if(x%2==0)
    {
        printf("%d is a enen number",x);
    }
    else
    {
        printf("%d is a odd number",x);
    }
}